// AttributeList doesn't have all the derived attribute types list vector<AttributeList>, so this
// file is only ever included explicitly.
#ifdef _NAPALM_INCLUDE_LIST
_NAPALM_TYPE_OP(AttributeList, AttributeList)
#endif
